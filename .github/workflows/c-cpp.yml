name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
 #   - name: cmake
 #     run: cmake -B build
    - name: make
      run: make clean && make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
